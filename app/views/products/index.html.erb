<p id="notice"><%= notice %></p>

<p>3rd design</p>

<h4 class="column">products</h4>
<div class="column">
  <ul class="products-grid no-bullet row">
    <% @products.each do |product| %>
      <li class="large-3 medium-4 small-12 column">
        <div class="products-pic padly">
          <%= link_to product, class: "poster" do %>
            <%= image_tag product.picture %>
          <% end %>
          <div class="product-info ell glassy-bg padmy padlx">
            <div class="title">
              <h6><%= product.name %> <span>(<%= product.created_at %>)</span></h6>
            </div>
            <p class="left price label movie-label radius">$ <%= product.price %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
</div>

<div style="margin: 100px;">
<hr>
<p>second design</p>
</div>

<h1>All Products</h1>

  <% @products.each do |product| %>
    <div class="productstemplate" style="display: inline-block; width: 200px; height: 200px; margin-left: 50px;" >
      <h1><%= product.name %></h1>
      <%= image_tag(product.picture_url , :width => 50) if product.picture.present? %>
      <p><%= product.description %></p>
      <h3><%= product.price %> $</h3>
      <h3><%= product.category %> $</h3>
      <div class="SED" style="display: inline;">
        <p><%= link_to 'Show', product %></p>

        <% if product.user == current_user %>
          <p><%= link_to 'Edit', edit_product_path(product) %></p>
          <p><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
        <%end%>
      </div>
    </div>
  <%end%>





<hr>
<p>first design</p>


  <div class="table" style="margin-top: 100px;">
    <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Picture</th>
        <th>Description</th>
        <th>Price</th>
        <th>Category</th>
        <th>user</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td><%= product.name %></td>
          <td><%= image_tag(product.picture_url , :width => 50) if product.picture.present? %></td>
          <td><%= product.description %></td>
          <td><%= product.price %></td>
           <td><%= product.category %></td>
          <th><%= product.user.name %></th>
          <td><%= link_to 'Show', product %></td>

          <% if product.user == current_user %>
          <td><%= link_to 'Edit', edit_product_path(product) %></td>
          <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <%end%>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>

<%= link_to 'New Product', new_product_path %>
